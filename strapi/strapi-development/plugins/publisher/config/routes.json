{
  "routes": [
    {
      "method": "GET",
      "path": "/",
      "handler": "publisher.index",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/publish",
      "handler": "publisher.publish",
      "config": {
        "policies": ["plugins::users-permissions.ratelimit"]
      }
    }
  ]
}
